# Генераторы ОГЭ записывают страницы с LF. Не даём Windows превращать сборку
# в массовый дифф только из-за окончаний строк.
pages/oge/** text eol=lf
data/oge/phase-0-report.* text eol=lf

# Растровые справочные таблицы не являются текстом.
*.png binary
*.ico binary
