/**
 * Сколько баллов даёт каждое задание ОГЭ по химии.
 * Источник — «Ответы и критерии оценивания» в сборниках типовых вариантов
 * и демоверсиях ФИПИ.
 */

/**
 * examType → максимальный балл.
 * По 1 баллу: 1–3, 5–8, 11, 13–16, 18, 19. По 2 балла: 4, 9, 10, 12, 17.
 * Задания 20–22 — по 3 балла, задание 23 — 5.
 */
export const POINTS = {
  1: 1,
  2: 1,
  3: 1,
  4: 2,
  5: 1,
  6: 1,
  7: 1,
  8: 1,
  9: 2,
  10: 2,
  11: 1,
  12: 2,
  13: 1,
  14: 1,
  15: 1,
  16: 1,
  17: 2,
  18: 1,
  19: 1,
  20: 3,
  21: 3,
  22: 3,
  23: 5,
};

/**
 * Пояснение, как балл теряется. У заданий на 2 балла одна ошибка стоит балла,
 * две — всех; у задания 23 балл складывается из двух критериев.
 */
export const POINT_NOTES = {
  2: "Полный правильный ответ — 2 балла; одна ошибка — 1 балл; две ошибки и более — 0.",
  5: "Складывается из двух критериев: уравнения реакций — до 2 баллов, оформление результатов опытов — до 3 баллов.",
};

const PLURAL = ["баллов", "балл", "балла", "балла", "балла", "баллов"];

/** «1 балл», «2 балла», «5 баллов» */
export function pointsLabel(examType) {
  const n = POINTS[examType];
  if (!n) return null;
  return `${n} ${PLURAL[n] ?? "баллов"}`;
}

/** Пояснение про частичный балл — только там, где оно есть. */
export function pointsNote(examType) {
  return POINT_NOTES[POINTS[examType]] ?? null;
}
